class Order {
    constructor(userId, items, totalAmount, address) {
      this.userId = userId;
      this.items = items;
      this.totalAmount = totalAmount;
      this.address = { address };
    }
  }
  
  export default Order;
  